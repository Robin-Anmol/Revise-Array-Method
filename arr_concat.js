//  array.concat() method use to combine two array; and more ;

const a = [56, 4, 3, 3, 2, 6];
const b = [67, 79, 4, 3, 2];
// console.log(a.concat(b));
// use to combine to araray ; console.log

const c = [55, 4, 5];
const d = a.concat(b, c);
// console.log(d);cat method we can use as a push method
d.concat(1.3, 2, 3, 4, 5);
console.log(d.concat(1.3, 2, 3, 4, 5));

// con
